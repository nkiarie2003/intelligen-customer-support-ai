# Training data

The original synthetic `complaints.csv` has been removed.

Run `python scripts/prepare_datasets.py` after downloading the Kaggle datasets. The training pipeline now uses `data/processed/cfpb_complaints.csv`.
